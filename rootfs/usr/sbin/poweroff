#!/bin/sh
# Stub for PDM's "Poweroff" UI button (and any code path that exec's
# `poweroff`). See reboot stub for rationale.
echo "[poweroff-stub] poweroff requested but suppressed: this PDM is a container." >&2
echo "[poweroff-stub] Stop with 'docker compose down' from the host." >&2
exit 0
